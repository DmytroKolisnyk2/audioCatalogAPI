export enum Refs {
  TEST = 'test',
  USER = 'User',
  LANGUAGES = "['en', 'ua']", // You have to use JSON.parce to get array
  THEMES = "['light', 'dark']", // You have to use JSON.parce to get array
}
